package com.rakuten.mobile.server.domain;

/**
 * Enum representing the supported question types for the MVP.
 * Defines types like SINGLE_CHOICE, MULTI_CHOICE, TEXT, and NUMBER.
 */
public enum QuestionType {
    SINGLE_CHOICE,
    MULTI_CHOICE,
    TEXT,
    NUMBER
}
